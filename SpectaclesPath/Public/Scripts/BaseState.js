// -----JS CODE-----
// BASE STATE
// Use a copy of this to start, run, and exit a sub-section of your program

// Register with state machine
// Change the state name to a unique ID in the enum set in StateMachine
script.stateName = global.stateName.hasNotBeenSet;
global.stateMachine.register(script);

// ENTER ----------------------------------
script.enter = function () {
};

// PROCESS ----------------------------------
script.process = function () {
};

// EXIT ----------------------------------
script.exit = function () {
};
